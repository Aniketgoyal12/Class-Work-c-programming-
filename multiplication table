// C program to print multiplication table of a number.//

#include <stdio.h>
int main()

{
    int a,b;
    printf("Enter the number to calculate its table");
    scanf("%d",&a);
    for(b=1;b<=10;b++)
    {
        printf("\n%d x %d = %d",a,b,b*a);
    }
    return 0;
}
