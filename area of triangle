#include <stdio.h>
//Program to find area of triangle with given height and base//
int main()
{
    int h,b,a;
    printf("Enter height and base of triangle respectively");
    scanf("%d%d", &h,&b);
    
    a = h*b/2;
    
    printf("Area of the triangle = %d",a);
    
    return 0;
}
