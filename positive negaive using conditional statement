#include <stdio.h>

int main()

{
    int num;
    printf("Enter the number");
    scanf("%d",&num);
    
    if (num >= 0)
    {
        printf("Number is positve");
    }
    else
    {
        printf("Number is negative");
    }
    return 0 ;
}
